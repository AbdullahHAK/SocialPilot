// Data model lives here. Models are added in Phase 1 (multi-tenant schema),
// then extended additively in later phases — see docs/roadmap.md.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
